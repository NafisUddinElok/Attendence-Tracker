name: attendance_backend
description: GPS-Geofenced University Attendance System — Dart Frog Backend
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  dart_frog: ^1.1.0
  postgres: ^3.5.3
  dart_jsonwebtoken: ^2.8.1
  crypto: ^3.0.3
  bcrypt: ^1.1.3
  dotenv: ^4.2.0
  uuid: ^4.5.1
  collection: ^1.18.0

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.0
  test: ^1.0.0
